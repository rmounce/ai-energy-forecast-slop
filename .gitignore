# Generated files, and private config
*.json
*.csv

# Trained model
*.pkl
*.log
TransformerApplicationNEM/

# Python
__pycache__/
*.pyc
scratch.py
test.py
.venv/
.env

# price-history.ipynb NEMOSIS
nemosis_cache/
data/nemseer_cache/
data/http_cache.sqlite

# Scratch/working data
scratch/
.codex
.codex/

# Local eval scratch artifacts
eval/results/holistic_eval_raw_*_hybrid.parquet.run*

# ML data files (rebuilt from InfluxDB via data/export_parquet.py)
data/parquet/
*.npy
*.npz
*.parquet

# Trained model checkpoints (large binaries)
models/
*.pt

# Explicitly include
!config.json
!config.secrets.json.example
!config.example.json
!tests/fixtures/*.json

# Local/generated eval and reviewer scratch artifacts. Tracked historical
# eval/results files remain tracked; new generated outputs should be added
# explicitly with `git add -f` only when they are deliberately preserved.
eval/results/*
docs/review_*.md
